using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LogNavigationManager : MonoBehaviour
{

    bool playerIsAbove;
    bool playerIsBelow;

    // Start is called before the first frame update
    void Start()
    {
        playerIsAbove = false;
        playerIsBelow = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
